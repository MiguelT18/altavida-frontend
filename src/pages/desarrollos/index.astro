---
import Header from "@components/Header.astro";
import Layout from "../../layouts/Layout.astro";
import MainSection from "@components/MainSection.astro";
import ContactLayout from "src/layouts/ContactLayout.astro";
import "@src/index.css";
---

<Layout title="AltaVida - Desarrollos 🔨">
  <Header title="Desarrollos" subtitle="Explora a fondo nuestros proyectos" />

  <MainSection>
    <h1 class="text-black text-lg text-center font-bold uppercase">
      Conoce nuestros desarrollos en Tarija
    </h1>
    <span
      class="bg-black block mx-auto w-[60%] max-w-[500px] h-[2px] relative after:block after:w-[30%] after:h-[5px] after:mx-auto after:bg-orange mt-8 mb-10"
    ></span>

    <div class="max-w-[700px] mx-auto mb-10">
      <p class="text-sm text-center">
        Descubre la excelencia de AltaVida en proyectos que abarcan desde lujo
        con <strong>Bless</strong> hasta versatilidad con
        <strong> Río Vida</strong> y confiabilidad con
        <strong> Altamira</strong>. Nuestra experiencia de nueve años, más de
        350 casas construidas y servicios de intermediación financiera nos
        distinguen. Explora nuestras opciones con un compromiso inquebrantable
        con la excelencia, versatilidad e integridad.
      </p>
    </div>

    <div class="imageButton_container">
      <a class="relative flex justify-center" href="/desarrollos/altamira">
        <img
          transition:name="altamira-image"
          class="max-w-[400px] transition-all"
          src="/src/media/images/pages/desarrollos/altaMira/renderCard.png"
          alt="Alta Mira render button"
          loading="lazy"
          decoding="async"
        />
        <div
          class="absolute bottom-10 text-white font-bold z-10 uppercase text-center pb-2"
        >
          <h4 transition:name="altamira-title" class="mb-3">Alta Mira</h4>
          <img
            class="max-w-[50px]"
            src="/src/media/logos/projects/Logo_AltaMira.webp"
            alt="Alta Mira Logotipo"
            loading="lazy"
            decoding="async"
          />
        </div>
      </a>

      <a class="relative flex justify-center" href="/desarrollos/bless">
        <img
          transition:name="bless-image"
          class="max-w-[400px] transition-all"
          src="/src/media/images/pages/desarrollos/bless/bless_portada.jpg"
          alt="Bless render button"
          loading="lazy"
          decoding="async"
        />
        <div
          class="absolute bottom-10 text-white font-bold z-10 uppercase text-center pb-2"
        >
          <h4 transition:name="bless-title" class="mb-3">Bless</h4>
          <img
            class="max-w-[60px]"
            src="/src/media/logos/projects/Logo_Bless.webp"
            alt="Bless Logotipo"
            loading="lazy"
            decoding="async"
          />
        </div>
      </a>

      <a class="relative flex justify-center" href="/desarrollos/riovida">
        <img
          transition:name="riovida-image"
          class="max-w-[400px] transition-all"
          src="/src/media/images/pages/desarrollos/rioVida/rioVida_portada.png"
          alt="RioVida render button"
          loading="lazy"
          decoding="async"
        />
        <div
          class="absolute bottom-10 text-white font-bold z-10 uppercase text-center pb-2"
        >
          <h4 transition:name="riovida-name" class="mb-3">Río Vida</h4>
          <img
            class="max-w-[60px]"
            src="/src/media/logos/projects/Logo_RioVida.webp"
            alt="Alta Mira Logotipo"
            loading="lazy"
            decoding="async"
          />
        </div>
      </a>
    </div>
  </MainSection>

  <ContactLayout />
</Layout>

<style>
  .imageButton_container {
    max-width: 680px;
    margin: 0 auto;
    display: grid;
    justify-items: center;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 1rem;
    animation: fadeIn 0.2s ease-out;
  }
  .imageButton_container img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .imageButton_container a {
    position: relative;
    box-shadow: 0 8px 11px rgba(0, 0, 0, 0.2);
    transition: transform 0.3s ease-out;
  }
  .imageButton_container a::before {
    position: absolute;
    top: 18px;
    left: 18px;
    width: calc(100% - 35px);
    height: calc(100% - 35px);
    content: "";
    display: block;
    z-index: 2;
    border: 1px solid #fff;
    opacity: 0.8;
  }
  .imageButton_container a::after {
    content: "";
    top: 0;
    position: absolute;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      0deg,
      rgba(0, 0, 0, 0.75) 0%,
      rgba(81, 165, 194, 0) 100%
    );
  }
  .imageButton_container a:nth-child(2) {
    grid-row-start: 1;
    grid-row-end: 3;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: scale(0.5);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }
</style>

<script>
  document.addEventListener("astro:page-load", () => {
    const buttonContainer = document.querySelectorAll(
      ".imageButton_container a",
    );
    const observerOptions = {
      root: null,
      rootMargin: "190px",
      threshold: 0.9,
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        const { isIntersecting } = entry;
        if (isIntersecting) {
          entry.target.classList.add("fade-in");
        } else {
          entry.target.classList.remove("fade-in");
        }
      });
    }, observerOptions);

    buttonContainer.forEach((button) => {
      observer.observe(button);
      button.addEventListener("mouseenter", () => {
        button.classList.remove("to-shrink");
        button.classList.add("to-grow");
      });
      button.addEventListener("mouseleave", () => {
        button.classList.remove("to-grow");
        button.classList.add("to-shrink");
      });
    });
  });
</script>
