---
import Header from "@components/Header.astro";
import Layout from "../../layouts/Layout.astro";
import MainSection from "@components/MainSection.astro";
import ContactLayout from "src/layouts/ContactLayout.astro";
import SearchForm from "@components/SearchForm.jsx";
import Icon from "astro-icon";

// TODO: Separar las cards de los modelos en un componente y darle funcionalidad a la barra de búsqueda
---

<Layout title="AltaVida - Catálogo 📦">
  <Header
    title="Catálogo"
    subtitle="Explora a fondo nuestros modelos de construcción"
  />
  <MainSection>
    <h1 class="text-black text-lg text-center font-bold uppercase">
      Encuentra el modelo que más se adapte a tus necesidades
    </h1>
    <span
      class="bg-black block mx-auto w-[60%] max-w-[500px] h-[2px] relative after:block after:w-[30%] after:h-[5px] after:mx-auto after:bg-orange mt-8 mb-14"
    ></span>
    <!-- <Icon
      name="ic:baseline-search"
      class="text-gray-300 w-[30px] absolute right-6 top-1/2 transform -translate-y-1/2 cursor-pointer"
    /> -->
    <SearchForm />
  </MainSection>
</Layout>

<ContactLayout />

<!-- <script>
  document.addEventListener("astro:page-load", () => {
    const observerOptions = {
      root: null,
      rootMargin: "100px", // en cuanto se vea el elemento
      threshold: 0, // porcentaje de visibilidad
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        const { isIntersecting } = entry;
        if (isIntersecting) {
          entry.target.classList.add("fade-in");
        } else {
          entry.target.classList.remove("fade-in");
        }
      });
    }, observerOptions);

    document.querySelectorAll("#modelo-card").forEach((element) => {
      observer.observe(element);
    });
  });
</script> -->

<style>
  .modelosContainer {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    justify-items: center;
    gap: 3rem;
  }

  .fade-in {
    animation: fadeIn 0.3s ease-out;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: scale(0.5);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }
</style>
